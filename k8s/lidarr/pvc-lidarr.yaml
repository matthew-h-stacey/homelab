apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-lidarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-lidarr
  storageClassName: sc-truenas1-media