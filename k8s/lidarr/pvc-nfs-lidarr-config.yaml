apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nfs-lidarr-config
  namespace: lidarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-nfs-lidarr-config
  storageClassName: sc-nfs-lidarr-config