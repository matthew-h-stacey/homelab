apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-lh-sonarr-config
  namespace: lidarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-lh-lidarr-config
  storageClassName: sc-nfs-lidarr-config