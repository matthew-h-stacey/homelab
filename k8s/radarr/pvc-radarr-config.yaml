apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-radarr-config
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-nfs-config-radarr
  storageClassName: sc-nfs-truenas1-config