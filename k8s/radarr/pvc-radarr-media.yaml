apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-lidarr-media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-nfs-media-lidarr
  storageClassName: sc-nfs-truenas1-media